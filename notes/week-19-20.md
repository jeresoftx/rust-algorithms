# Semanas 19 y 20: Tries y Cadenas

## Objetivo

Construir la base de tries para resolver búsquedas por prefijo, diccionarios con comodines y reemplazo de palabras por raíces.

## Estado del Avance

- Problemas implementados acumulados: 114.
- Tests automatizados acumulados: 279.
- Módulo agregado: `src/patterns/tries.rs`.
- Tests agregados: `tests/tries_test.rs`.
- Módulo agregado: `src/patterns/string_algorithms.rs`.
- Tests agregados: `tests/string_algorithms_test.rs`.

## Problemas Implementados

| Problema | Patrón | API | Estado |
| --- | --- | --- | --- |
| Implement Trie | Trie básico | `Trie` | implementado |
| Design Add and Search Words Data Structure | Trie + DFS con comodín | `WordDictionary` | implementado |
| Replace Words | Trie + prefijo más corto | `replace_words` | implementado |
| KMP Pattern Search | KMP | `find_pattern_positions` | implementado |
| Find All Anagrams in a String | Ventana + frecuencias | `find_anagram_starts` | implementado |
| Repeated Substring Pattern | KMP conceptual / string doubling | `repeated_substring_pattern` | implementado |
| Longest Duplicate Substring | Búsqueda binaria + ventanas repetidas | `longest_duplicate_substring` | implementado |

## Ideas Clave

- Cada arista representa un carácter.
- `is_word` separa "este prefijo existe" de "esta palabra existe".
- Un comodín `.` se resuelve probando todos los hijos posibles en ese nivel.
- Para reemplazar palabras, conviene devolver la primera raíz completa encontrada durante el recorrido.
- KMP evita retroceder en el texto usando una tabla de prefijos del patrón.
- Una ventana de anagramas mantiene conteos y mueve un carácter de entrada por uno de salida.
- Longest Duplicate Substring puede buscar la longitud máxima con búsqueda binaria y verificar duplicados con ventanas.
- La versión actual es útil para práctica, aunque un rolling hash o suffix array mejora el costo para entradas grandes.

## Complejidad

| Operación | Tiempo | Espacio |
| --- | ---: | ---: |
| Insertar palabra | `O(L)` | `O(L)` en nuevos nodos |
| Buscar palabra exacta | `O(L)` | `O(1)` adicional |
| Buscar prefijo | `O(P)` | `O(1)` adicional |
| Buscar con comodines | `O(26^W * L)` en peor caso | `O(L)` por recursión |
| Reemplazar oración | `O(total de caracteres)` | `O(total del diccionario)` |
| KMP | `O(n + m)` | `O(m)` |
| Anagramas en ventana | `O(n + m)` | `O(1)` para alfabeto fijo |
| Patrón repetido | `O(n)` promedio por búsqueda interna | `O(n)` |
| Longest Duplicate Substring | `O(n^2 log n)` peor caso | `O(n)` |

`L` es la longitud de la palabra, `P` la longitud del prefijo, `W` la cantidad de comodines, `n` la longitud del texto y `m` la longitud del patrón.

## Criterio de Cierre

- `Trie` soporta `insert`, `search` y `starts_with`.
- `WordDictionary` soporta búsqueda con `.`.
- `replace_words` usa la raíz más corta.
- `find_pattern_positions` encuentra coincidencias traslapadas.
- `find_anagram_starts` encuentra todas las ventanas con la misma frecuencia.
- `repeated_substring_pattern` detecta repetición de unidad base.
- `longest_duplicate_substring` cubre duplicados largos, ausencia de duplicados y cadenas pequeñas.
- README y wiki apuntan al bloque.
- `cargo fmt` y `cargo test` pasan.

## Siguiente Paso

Pasar a grafos ponderados: Dijkstra y Bellman-Ford.
